name: Test release of action

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master, test-action ]
  workflow_dispatch:

jobs:
  test_stale:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: rodvdka/git-sweep-merged-and-stale@v0.12-alpha
